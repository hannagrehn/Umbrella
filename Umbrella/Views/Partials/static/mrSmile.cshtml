@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage;
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@inject Umbraco.Cms.Web.Common.UmbracoHelper UmbracoHelper;

@{
    var rootContents = UmbracoHelper.ContentAtRoot();
    var settings = rootContents?.FirstOrDefault(x => x.ContentType.Alias == "settingsPage") ?? null;
    var officeLocation  = settings?.Value<string>("officeLocation") ?? ""; 
    var officePhoneNumber = settings?.Value<string>("officePhoneNumber") ?? ""; 
    var officeEmailAddress = settings?.Value<string>("officeEmailAddress") ?? ""; 
    var smile = Umbraco.Media(Guid.Parse("0b1b4d44-c803-447d-bf48-bbfe99c2204b")) as Image;
    var triangle = Umbraco.Media(Guid.Parse("9fa3f88c-b3d5-4d3d-b935-33181f6395b3")) as Image;
     
}

<section id="mr-smile">
    <div class="container">
        <div class="picture">
            <img src="@smile.Url()" />            
        </div>
        <div class="triangle">
            <img src="@triangle.Url()" />           
        </div>
        <div class="text">
            <div class="headline">
                <h2>Let us know about your next project</h2>
            </div>
            <div class="location">
                <i class="fa-regular fa-location-dot"></i>
                <div class="info">
                    <h5>Office location</h5>
                    <p>@officeLocation</p>            
                </div>
            </div>
            <div class="phone">
                <i class="fa-light fa-phone-flip"></i>
                <div class="info">
                    <h5>Phone number</h5>
                    <p>@officePhoneNumber</p>            
                </div>
            </div>
            <div class="email">
                <i class="fa-regular fa-envelope"></i>
                <div class="info">
                    <h5>Email address</h5>
                    <p>@officeEmailAddress</p>     
                </div>
            </div>
        </div>
    </div>
</section>
